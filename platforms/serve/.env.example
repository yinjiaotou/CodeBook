NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://pwdlock:pwdlock@localhost:5432/pwdlock
JWT_SECRET=replace-with-at-least-32-random-bytes
JWT_TTL_SECONDS=900
# Keep false for every shared, staging, or production database. Migrations own schema changes.
TYPEORM_SYNCHRONIZE=false
