<?xml version="1.0" encoding="utf-8"?>
<!--
  开发期调试用：仅对后端服务所在主机放行明文 HTTP（cleartext）。
  本地 serve：模拟器通过 10.0.2.2 访问宿主机；真机可用 `adb reverse tcp:3000 tcp:3000` 后使用 localhost。
  云服务器（部署环境）：124.223.115.40:3000，本地开发默认直连。
  生产环境必须使用 HTTPS，且不应依赖此配置。
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">124.223.115.40</domain>
    </domain-config>
</network-security-config>
